# This file was auto-generated by the Firebase CLI
# https://github.com/firebase/firebase-tools

name: Deploy to Firebase Hosting on merge
on:
  push:
    branches:
      - main
jobs:
  build_and_deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: npm ci
      - run: |
          echo "export const environment = {
            firebaseConfig: {
              apiKey: '${{ secrets.FIREBASE_API_KEY }}',
              authDomain: 'big-query-test-uni.firebaseapp.com',
              projectId: 'big-query-test-uni',
              storageBucket: 'big-query-test-uni.firebasestorage.app',
              messagingSenderId: '${{ secrets.FIREBASE_MESSAGING_SENDER_ID }}',
              appId: '${{ secrets.FIREBASE_APP_ID }}',
              measurementId: 'G-C9X8M3NN3V'
            },
            appCheckToken: '${{ secrets.APP_CHECK_TOKEN }}'
          };" > src/environments/environment.ts
      - run: npm run build
      - uses: FirebaseExtended/action-hosting-deploy@v0
        with:
          repoToken: ${{ secrets.GITHUB_TOKEN }}
          firebaseServiceAccount: ${{ secrets.FIREBASE_SERVICE_ACCOUNT_BIG_QUERY_TEST_UNI }}
          channelId: live
          projectId: big-query-test-uni
